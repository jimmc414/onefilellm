# Test Dependencies for OneFileLLM
# Install with: pip install -r requirements-test.txt

# Core testing frameworks
pytest>=7.0.0
pytest-snapshot>=0.9.0
pytest-xdist>=3.0.0  # For parallel test execution
pytest-timeout>=2.0.0
pytest-mock>=3.0.0
pytest-cov>=4.0.0

# Coverage tools
coverage>=7.0.0
coverage-badge>=1.1.0

# Additional testing utilities
responses>=0.23.0  # For mocking HTTP requests
freezegun>=1.2.0  # For mocking time/dates
parameterized>=0.9.0  # For parameterized tests

# Code quality tools (optional but recommended)
flake8>=6.0.0
black>=23.0.0
mypy>=1.0.0
isort>=5.12.0

# Performance testing
pytest-benchmark>=4.0.0  # For benchmark tests
memory-profiler>=0.61.0  # For memory usage testing

# Documentation testing
pytest-doctest-custom>=1.0.0  # For testing code in docstrings

# CI/CD integration
junit-xml>=1.9  # For JUnit XML reports
pytest-html>=3.2.0  # For HTML reports
pytest-json-report>=1.5.0  # For JSON reports